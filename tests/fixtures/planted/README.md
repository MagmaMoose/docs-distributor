Every other file in this directory plants exactly one leak the audit must catch (see
tests/test_acceptance.py). The values are synthetic or public, and the repo's own
private-value hook skips this directory for the pattern layer on purpose.
